# 复制成 u60.env 再填：cp u60.env.example u60.env
# install.sh 启动时自动读取，这样密码不用在终端里输，也不用告诉 Claude。装完可以删掉 u60.env。
# 命令行/环境变量里给了同名变量时，以命令行为准。
# 密码里有单引号的话，改用双引号括起来，并在 $ ` \ " 前面加反斜杠。

# 路由器管理密码（登录 http://192.168.0.1 用的）
ROUTER_PASSWORD=''

# 高级后台密码，不能含引号、反斜杠、空格。
# 保持这行被 # 注释掉 = 和路由器管理密码一样；
# 去掉 # 但留空 = 沿用设备上已有的后台密码（只适合重装，第一次装会失败）。
# AGENT_PASSWORD=''

# U60 地址不是 192.168.0.1 时再填
# GATEWAY='192.168.0.1'
